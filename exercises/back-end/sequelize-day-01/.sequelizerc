
const path = require('path');

module.exports = {
  'config': path.resolve('src', 'config', 'config.js'),
  'models-path': path.resolve('src', 'models'),
  'seeders-path': path.resolve('src', 'seeders'),
  'migrations-path': path.resolve('src', 'migrations'),
};

// path: módulo interno do Node que nos fornece utilitários para trabalharmos com caminhos de arquivos e diretótios;
// config: é um caminho para o caminho de configuração;
// model-path: é um caminho para o diretṕrio models;
// seeders-path: caminho para dir seeders;
// migration-path: cami para dir migrations